export const isTheSame = (firstElement:any, secondElement:any) =>{
    return firstElement === secondElement
}

export const minusElements = (firstElement: number, secondElement: number) =>{
    return firstElement - secondElement
}


export const calculatePercent = (state: number, count: number) =>{
    return (state / count) * 100
}
